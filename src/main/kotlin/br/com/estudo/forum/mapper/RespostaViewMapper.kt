/*
package br.com.estudo.forum.mapper

import br.com.estudo.forum.dto.RespostaView
import br.com.estudo.forum.model.Resposta
import br.com.estudo.forum.service.TopicoService
import br.com.estudo.forum.service.UsuarioService
import org.springframework.stereotype.Component

@Component
class RespostaViewMapper(private var autorService: UsuarioService,
                        private var topicoService: TopicoService)
    :Mapper<Resposta,RespostaView> {

    override fun mapper(r: Resposta): RespostaView {
       */
/*return RespostaView(
           mensagem = r.mensagem,
           idAutor  = autorService.buscarPorId(r.id),
           solucao  = r.solucao,
           idTopico = topicoService.buscarPorId(r.idTo)
       )*//*

        val resposta = RespostaView

        //resposta = null

        return resposta

    }
    }
*/
